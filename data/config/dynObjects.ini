[StoreIDTrack]
0 = > Object.GetName ^, > Get < ID, Set Runtime < ^ ; This will store the object's GUID in the Runtime section under a key defined on the object itself
Immediate = true

[Scene]
TrackList = StoreIDTrack
ID = Scene

[MainGameScene@Scene]

[DynamicObject]
TrackList = DynamicTrack

[DynamicTrack]
0 = > Get Runtime Scene, Object.SetOwner ^ < ; This will set the current Scene as the owner of this object
Immediate = true

[DynObj1@DynamicObject]

[OtherDynObj@DynamicObject]